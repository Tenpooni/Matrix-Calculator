package persistence;

import model.Row;
import model.Column;

import static org.junit.jupiter.api.Assertions.assertEquals;

public class JsonTest {
    protected void checkValue(int index, Row row) {
        //stub
    }
}